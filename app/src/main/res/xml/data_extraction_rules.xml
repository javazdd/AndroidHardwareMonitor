<?xml version="1.0" encoding="utf-8"?>
<!--
    Blocks all cloud backup and device-to-device transfer of app data.
    SharedPreferences contains the API key; it must never leave the device
    via Google Backup, ADB backup, or Android's D2D transfer mechanism.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database"   path="." />
        <exclude domain="file"       path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database"   path="." />
        <exclude domain="file"       path="." />
    </device-transfer>
</data-extraction-rules>
